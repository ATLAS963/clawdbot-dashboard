{
  "name": "clawdbot-dashboard",
  "version": "2.0.0",
  "description": "Task tracking dashboard for ClawdBot — Vercel + Supabase",
  "private": true,
  "scripts": {
    "build": "echo 'Static site — no build step'",
    "dev": "vercel dev"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.49.1"
  }
}
